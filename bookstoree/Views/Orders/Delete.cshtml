@model bookstoree.Models.Order

@{
    ViewData["Title"] = "Xóa đơn hàng";
    Layout = "_Layout";
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6" style="display: flex ; justify-content: flex-end;">
                <h1>Xóa đơn hàng</h1>
            </div>
            <div class="col-sm-6" style="display: flex ; justify-content: flex-end;">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Trang chủ</a></li>
                    <li class="breadcrumb-item active">Xóa</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card card-danger">
                    <div class="card-header">
                        <h3 class="card-title">Bạn có chắc chắn muốn xóa mục này không?</h3>
                    </div>
                    <div class="card-body">
                        <h4>Đơn hàng</h4>
                        <hr />
                        <dl class="row">
                            <dt class = "col-sm-2">
                                @Html.DisplayName("OrderDate")
                            </dt>
                            <dd class = "col-sm-10">
                                @Model.OrderDate
                            </dd>
                            <dt class = "col-sm-2">
                                @Html.DisplayName("Status")
                            </dt>
                            <dd class = "col-sm-10">
                                @Model.Status
                            </dd>
                            <dt class = "col-sm-2">
                                @Html.DisplayName("PaymentMethod")
                            </dt>
                            <dd class = "col-sm-10">
                                @Model.PaymentMethod
                            </dd>
                            <dt class = "col-sm-2">
                                @Html.DisplayName("TotalAmount")
                            </dt>
                            <dd class = "col-sm-10">
                                @string.Format("{0:N0} VNĐ", Model.TotalAmount)
                            </dd>
                            <dt class = "col-sm-2">
                                @Html.DisplayName("User")
                            </dt>
                            <dd class = "col-sm-10">
                                @(Model.User != null ? Model.User.UserId.ToString() : "N/A")
                            </dd>
                            <dt class = "col-sm-2">
                                @Html.DisplayName("Discount")
                            </dt>
                            <dd class = "col-sm-10">
                                @(Model.Discount != null ? Model.Discount.DiscountCodeId : "N/A")
                            </dd>
                        </dl>

                        <hr />
                        <!-- <h4>Order Items</h4> -->
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Tên sách</th>
                                    <th>Số lượng</th>
                                    <th>Tổng tiền</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.OrderDetails != null)
                                {
                                    @foreach (var item in Model.OrderDetails)
                                    {
                                        <tr>
                                            <td>@(item.Book != null ? item.Book.Title : "N/A")</td>
                                            <td>@item.Quantity</td>
                                            @* <td>@string.Format("{0:N0} VNĐ", item.UnitPrice)</td> *@
                                            <td>@string.Format("{0:N0} VNĐ", item.Quantity * item.UnitPrice)</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="card-footer">
                        <form asp-action="Delete">
                            <input type="hidden" asp-for="OrderId" />
                            <input type="submit" value="Xóa" class="btn btn-danger" /> |
                            <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
